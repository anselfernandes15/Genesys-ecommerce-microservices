services:
  postgres:
    image: postgres:16
    container_name: ecommerce-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password123
      POSTGRES_DB: productdb
    ports:
      - "5555:5432"

volumes: {}